#pragma once 
#include <string>

namespace Combo
{
    
    class Shader
    {
        public:

            ~Shader();

            void Bind() const;
            void Unbind() const;

            
        private:

    };
}